apiVersion: v1
kind: Service
metadata:
  name: ops-app1
  namespace: ops-app1-eks-dev
  labels: # ← 줄바꿈
    app.kubernetes.io/name: ops-app1 # ← 들여쓰기 추가
spec:
  selector:
    app.kubernetes.io/name: ops-app1
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
